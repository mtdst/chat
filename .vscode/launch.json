{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/chat/.",
            "env": {},
            "args": [
                "--workspace_folder=${workspaceFolder}",
                "--port=8080",
            ]
        }
    ]
}